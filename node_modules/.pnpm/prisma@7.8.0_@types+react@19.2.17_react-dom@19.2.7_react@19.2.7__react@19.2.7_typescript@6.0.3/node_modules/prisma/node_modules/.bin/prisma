#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*|*MINGW*|*MSYS*)
        if command -v cygpath > /dev/null 2>&1; then
            basedir=`cygpath -w "$basedir"`
        fi
    ;;
esac

if [ -x "$basedir/node" ]; then
  exec "$basedir/node"  "$basedir/../../build/index.js" "$@"
else
  exec node  "$basedir/../../build/index.js" "$@"
fi
# cmd-shim-target=/home/rene/restaurant-booking-saas/node_modules/.pnpm/prisma@7.8.0_@types+react@19.2.17_react-dom@19.2.7_react@19.2.7__react@19.2.7_typescript@6.0.3/node_modules/prisma/build/index.js
